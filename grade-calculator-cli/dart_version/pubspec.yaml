name: grade_calculator
description: A CLI application to calculate grades and GPA from Excel files
version: 1.0.0
publish_to: none

environment:
  sdk: '>=2.19.0 <4.0.0'

dependencies:
  excel: ^4.0.3
  args: ^2.4.2

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0

