name: grade_calculator
description: A GUI and CLI application to calculate grades and GPA from Excel files
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  excel: ^4.0.3
  args: ^2.4.2
  file_picker: ^8.0.0+1
  path: ^1.8.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^3.0.0
  test: ^1.21.0
  flutter_lints: ^3.0.0

